let threeJSScene;

function InitMenu(scene)
{
    threeJSScene = scene;
    // scene.traverse( function( object ) {

    //     if ( object.isMesh ) console.log( object );
    
    // } );

}

export { InitMenu }